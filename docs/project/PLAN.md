# План работы

> План и история решений. [Навигация и актуальный срез](README.md). Датированные разделы описывают свой этап; ранние планы не подтверждают текущую готовность. Пути в командах указаны от корня репозитория.

Связанные документы: [CLAUDE.md](../../CLAUDE.md) | [ARCHITECTURE.md](ARCHITECTURE.md) | [AGENT.md](AGENT.md) | [требования к таблице](TABLE-VIEW-REQUIREMENTS.md) | [DEVLOG.md](../../DEVLOG.md).

## Текущая реализация

Восемь процессных скиллов подключены. Backend-пакет расширен до миграции 029; последующие проверки восьми сценариев и скорости описаны в [отчёте](../../analysis/lactis-prime-8/EIGHT-SCENARIOS.md). Предыдущая проверка до 028 включала: Ovsynch с привязкой к действующему назначению, счётчики, периоды молочных показателей и восемь контрольных серверных выборок. Фактические результаты и ещё не покрытые частные сценарии: [BACKEND-PROCESSES-PLAN.md](BACKEND-PROCESSES-PLAN.md). Предыдущий LLM-тест отбора по весу пройден; финальную проверку новых изменений через чат пользователь выполнит в интерфейсе. Автоматический LLM-тест не является условием этого checkpoint. Массовое расширение CSV не возобновляется.

## Завершённый исследовательский этап

План исследования процессов выбранной компании **Лактис Прайм 8**: [LACTIS-RESEARCH-PLAN.md](LACTIS-RESEARCH-PLAN.md). Исследовательский разбор выполнен: [результаты](../../analysis/lactis-prime-8/README.md). Все 259 списков и 28 чатов учтены, проведена ограниченная независимая проверка. Массовое расширение CSV-каталога не возобновлено. Скиллы впоследствии реализованы и проверены; остановка их разработки относилась к этому исследовательскому этапу. Ниже сохранён прежний технический этап.

## Предыдущая техническая цель

Текущая цель — подготовить инфраструктуру и данные, чтобы агент выбирал и полностью исполнял правила из `data/lists.csv`, показывая результат в существующей таблице. Спецификация: [CSV-RULES.md](CSV-RULES.md). Детальный план с зависимостями и приёмкой: [CSV-RULES-PLAN.md](CSV-RULES-PLAN.md).

Пользователь разрешил реализацию R00–R21. **Фактический прогресс и незакрытые критерии — [rules/implementation-status.md](../../rules/implementation-status.md); весь каталог ещё не готов.** Исходные CSV и прототип не меняются; требование сохранить 6 × 8 500 относится к исходному этапу; позднее «Лактис Прайм» изменена по отдельному решению пользователя. Расписания и редизайн не входят в этот этап.

## Трек Agent 4 — правила CSV и подготовка БД [IN PROGRESS]

| Этап | Задачи | Результат | Статус |
|---|---|---|---|
| 0 | R00–R02 | Проверенный baseline v5, все 6 735 определений и полная карта разрывов | [ ] |
| 1 | R03–R04 | Контракты каталога, полей, параметров и выполнения | [ ] |
| 2 | R05–R08 | Факты, формулы, синтетическая история и быстрые проекции | [ ] |
| 3 | R09–R11 | Парсер, импорт, bindings и безопасное исполнение | [ ] |
| 4 | R12–R14 | Поиск, чтение и применение правила агентом | [ ] |
| 5 | R15–R16 | Полный результат в принятой таблице и ручное продолжение | [ ] |
| 6 | R17–R19 | Проверка каждого определения, реальной LLM, доступа и скорости | [ ] |
| 7 | R20–R21 | Проверенное обновление/восстановление, итоговая поставка и push | [ ] |

Готовность определяется результатами каждого списка, а не наличием парсера или таблиц реестра. Неразрешённые условия, потерянные колонки и обрезанные группировки оставляют соответствующую запись открытой. Каталог живёт в продуктовой БД; `arka_test` остаётся источником фактов и определений полей. Предыдущие треки ниже сохраняются как история, не как подтверждение нового этапа.

## Phases

### Phase 0 — Подготовка [COMPLETED]

| № | Работа | Статус |
|---|---|---|
| 0.1 | Проверить отсутствие целевой папки и скопировать весь комплект | [x] |
| 0.2 | Сверить 69 исходных файлов по SHA-256 | [x] |
| 0.3 | Прочитать вводные, ограничения данных и правила прототипа | [x] |
| 0.4 | Создать CLAUDE.md, ARCHITECTURE.md, PLAN.md, DEVLOG.md | [x] |
| 0.5 | Проверить Python, чтение четырёх CSV, локальные ссылки и синтаксис внешних JS | [x] |
| 0.6 | Проверить переходы и отображение исходного прототипа в браузере | [x] |
| 0.7 | Повторно сверить исходники после подготовки | [x] |

Дизайн-система уже поставлена в prototype/shared/. Установка примитивов, смена токенов, внедрение тёмной темы, нового стека или сборщика не входят в подготовку.

### Phase 0A — Спецификация исходной базы [COMPLETED]

| № | Работа | Статус |
|---|---|---|
| 0A.1 | Зафиксировать три доменных типа: ферма, животное, событие животного | [x] |
| 0A.2 | Описать общую таблицу событий и типизированные таблицы деталей | [x] |
| 0A.3 | Описать типы событий, временную модель, целостность, доступ и масштабирование | [x] |
| 0A.4 | Задать критерии готовности будущей реализации | [x] |
| 0A.5 | Сверить модель с реальными списками и добавить идентификаторы, архив, приплод, пользовательские события и вычисляемые поля | [x] |

Результат: [DATABASE.md](DATABASE.md). Спецификация не добавляет реальную базу, генератор данных, Аркашу, списки или изменения прототипа.

### Phase 0B — Приватная резервная копия [COMPLETED]

| № | Работа | Статус |
|---|---|---|
| 0B.1 | Инициализировать отдельный Git-репозиторий с веткой `main` | [x] |
| 0B.2 | Создать приватный GitHub-репозиторий | [x] |
| 0B.3 | Зафиксировать и отправить полный проект | [x] |

Результат: приватный `origin` — [<repository>](<repository-url>). Публичная публикация запрещена.

## Трек Agent 0 — Предыдущий каркас [SUPERSEDED]

Минимальный Eve-стенд был проверен и опубликован, но не покрывал продуктовые требования. Его код заменён официальным Vercel AI Chatbot в треке Agent 1; старый deployment не считается текущей версией продукта.

## Трек Agent 1 — Полный чат-продукт [IN PROGRESS: EXTERNAL INPUTS]

| № | Работа | Статус |
|---|---|---|
| A1 | Перейти с минимального Eve-чата на официальный Vercel AI Chatbot | [x] |
| A2 | Реализовать историю, URL, список, создание, переключение, переименование и удаление чатов | [x] |
| A3 | Добавить документы и фотографии до 20 МБ | [x] |
| A4 | Хранить файлы в private Vercel Blob и отдавать только владельцу | [x] |
| A5 | Извлекать текст PDF, DOCX, XLSX, TXT, MD, CSV и JSON | [x] |
| A6 | Добавлять найденные документы в серверный контекст модели | [x] |
| A7 | Сделать постоянную правую панель и предпросмотр файлов | [x] |
| A8 | Заложить состояния панели для будущих списков и таблиц фермы | [x] |
| A9 | Подготовить OpenAI-compatible адаптер нашей модели | [x] |
| A10 | Подключить Neon к Vercel project | ожидается принятие условий Marketplace |
| A11 | Добавить base URL, model ID и credential нашей модели | ожидаются входы |
| A12 | Выполнить production deployment и сквозную проверку | после A10–A11 |
| A13 | Отправить код и документацию в приватный `origin/main` | [x] |

Локально пройдены форматирование, typecheck, production build и браузерный сценарий загрузки, приватного хранения, API списка файлов и предпросмотра. Полная готовность требует production Neon и реального потокового ответа выбранной модели.

## Трек Agent 2 — База фермы и интерактивная таблица [BASELINE COMPLETED LOCALLY]

Каноническая спецификация: [AGENT-DATA-UI.md](AGENT-DATA-UI.md). Этап не меняет исходные события: агент получает доступ к чтению и управляет только сохранённым представлением отчёта.

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| I1.1 | Добавить отдельное `FARM_DATABASE_URL` и серверный клиент базы фермы | Чатовая и фермерская базы подключаются независимо | [x] |
| I1.2 | Связать Auth.js user с `farm_access.subject_id` | Сервер получает только разрешённые фермы | [x] |
| I1.3 | Добавить исходный выбор фермы | Первая доступная ферма становится обычным съёмным фильтром; без него видны все фермы из `farm_access` | [x] |
| I2.1 | Создать типизированный каталог полей животных | Неизвестные поля и операторы отклоняются | [x] |
| I2.2 | Реализовать построитель параметризованных фильтров и сортировки | Нет пути для передачи моделью SQL | [x] |
| I2.3 | Добавить страницы строк и агрегаты | Выдача ограничена 200 строками и cursor-пагинацией | [x] |
| I3.1 | Добавить Drizzle-таблицу и миграцию `ReportView` | Состояние сохраняется в продуктовой базе | [x] |
| I3.2 | Реализовать View API с проверкой владельца | Представление создаётся, читается и обновляется только владельцем | [x] |
| I3.3 | Реализовать optimistic locking по `revision` | Устаревшая запись получает `VIEW_REVISION_CONFLICT` | [x] |
| I4.1 | Подключить Codex app-server и `ToolLoopAgent` к общим tools | Локальная подписка и API-модель используют один серверный контракт | [x] |
| I4.2 | Добавить tools чтения фермы и животных | Агент отвечает по фактическим данным разрешённых ферм в границах фильтров | [x] |
| I4.3 | Добавить `getViewState`, `updateView`, `openAnimal` | Агент управляет правой панелью без DOM-команд | [x] |
| I5.1 | Реализовать режим `table` в существующей правой панели | Отображаются строки, колонки и состояния загрузки/ошибки | [x] |
| I5.2 | Добавить первый table baseline с названием, отдельной фермой, вложенными фильтрами и клиентской группировкой | Baseline заменён итоговым v3 в F1–F18 | [x] |
| I5.3 | Добавить виртуализацию и cursor-пагинацию | Большой результат не загружается целиком | [x] |
| I6.1 | Передавать `viewId`, `revision`, выделение и ID загруженной страницы через chat transport | Агент видит сохранённое состояние и текущую страницу; точный viewport вынесен в F12 | [x] |
| I6.2 | Передавать изменения агента через `data-view-state` | Открытая таблица меняется во время ответа | [x] |
| I6.3 | Направить ручные и агентные изменения через один View service | Правила и номера версий совпадают | [x] |
| I7.1 | Создать проверочный объём: 6 ферм по 8–9 тысяч животных | Генератор воспроизводимо создаёт 48–54 тысячи животных | [x] |
| I7.2 | Проверить доступ, фильтры, конфликт версии и восстановление | API и браузерные сценарии проходят | [x] |
| I7.3 | Измерить p50/p95 прямых SQL-запросов count и первой страницы | Результаты записаны; p95 первой страницы после прогрева ≤ 1 секунды локально | [x] |
| I7.4 | Выполнить форматирование, typecheck и production build | Все обязательные проверки завершаются успешно | [x] |

Baseline завершён и заменён: F6 перенёс группировку на серверные узлы и отдельную cursor-пагинацию.

## Трек Agent 3 — представление таблицы [COMPLETED LOCALLY]

Канонические документы: [формальные требования](TABLE-VIEW-REQUIREMENTS.md) и [UX-исследование](FILTER-UX.md). Текущий контракт v5 использует дерево фильтров, одно поле группировки и единые операции интерфейса и агента.

### Этап 0 — контракт [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F0 | Зафиксировать UX, модель состояния, API, доступность, производительность и 22 сценария приёмки | `docs/project/TABLE-VIEW-REQUIREMENTS.md` однозначно отделяет прежний baseline от итогового v3 | [x] |

### Этап 1 — модель и операции [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F1 | Ввести `ReportView v3`: плоские типизированные фильтры, стабильные ID, `SortRule` и `GroupRule` | Схема выражает **И** между условиями, **ИЛИ** внутри списка значений, отрицательные операторы и диапазон | [x] |
| F2 | Расширить каталог полей типом редактора и источником значений | UI и агент получают один список совместимых полей, операторов и редакторов | [x] |
| F3 | Добавить миграцию v1/v2→v3 и атомарные операции add/update/remove/move по ID | Плоские старые view сохраняют результат; невыразимые `OR`/`NOT` отклоняются явно; независимые изменения не заменяют весь view | [x] |

Сначала выполняются F1–F3: без стабильных ID интерфейс и tools снова будут зависеть от индексных путей и полной замены view.

### Этап 2 — серверная выборка [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F4 | Реализовать валидацию и SQL для плоского `AND`, списков значений, отрицательных операторов, `NULL`, диапазонов и дат фермы | Operator/value matrix совпадает с эталонными запросами | [x] |
| F5 | Заменить offset на непрозрачный cursor, связанный с revision, fingerprint и snapshot данных | Между страницами нет дублей/пропусков; stale cursor отклоняется | [x] |
| F6 | Перенести группировку на серверные group nodes и cursor дочерних строк | Группа появляется один раз и полностью выдаёт свой count | [x] |

F4 зависит от F1–F2; F5 — от детерминированной сортировки; F6 — от F4–F5.

### Этап 3 — интерфейс представления [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F7 | Разделить сохранённое состояние и локальный черновик, добавить отмену и защиту от stale-response | Невалидный черновик не меняет таблицу; валидное значение атомарно увеличивает revision | [x] |
| F8 | Собрать toolbar из трёх действий, строку правил и простой фильтр | Условие создаётся за `поле → оператор → значение`; пустой редактор скрыт | [x] |
| F9 | Добавить типизированные редакторы и редактирование отдельных чипов | Диапазоны, даты, логические и справочные значения не вводятся технической строкой | [x] |
| F10 | Пересобрать сортировку и группировку | Приоритет сортировок меняется drag и кнопками; поле и направление группировки меняются без удаления | [x] |
| F11 | Сделать **Ферму** обычным фильтром и расширить RLS до всех записей `farm_access` | Снятый фильтр показывает все разрешённые фермы; чужая ферма остаётся недоступна | [x] |

F7 выполняется первым. F8–F11 используют общий контракт состояния и типизированные редакторы F9.

### Этап 4 — данные экрана и агент [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F12 | Добавить накопление cursor-страниц, автозагрузку, ручной fallback и retry | Три страницы дают 150 уникальных ID; старые ответы не меняют новый view | [x] |
| F13 | Считать фактический viewport и раскрытые group paths | В чат не попадают скрытые строки и весь клиентский кэш | [x] |
| F14 | Перевести UI и tools агента на общий набор операций | Эквивалентные ручная и агентная команды создают один список правил и результат | [x] |
| F15 | Реализовать rebase независимых операций и явный конфликт несовместимых | Конкурентная правка не теряется и не применяется молча | [x] |

F12 зависит от F5–F6; F13 — от F12; F14–F15 — от F3 и готового UI.

### Этап 5 — приёмка [COMPLETED]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F16 | Закрыть клавиатуру, focus, loading/empty/error/retry/conflict и ARIA | Все операции проходят без мыши; drag имеет кнопочную альтернативу | [x] |
| F17 | Добавить unit, API и browser-тесты миграции, multi-farm, фильтров, race, групп и страниц | Выполнены `T-01`–`T-15` из требований | [x] |
| F18 | Прогнать полный gate на 51 000 животных и визуально проверить стенд | Выполнены `T-16`–`T-22`; фактический интерфейс соответствует спецификации | [x] |

### Этап 6 — упрощение группировки и геометрии v4 [IN PROGRESS]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F19 | Ограничить `groupBy` одним правилом в UI, agent tool, API, query и БД | Второй уровень недоступен и отклоняется всеми внешними контурами | [x] |
| F20 | Мигрировать v3 и сохранить отброшенные поля группировки как колонки | Остаётся внешний уровень, `revision` растёт, старый cursor становится stale | [x] |
| F21 | Выровнять панели и сложенное боковое меню по `46 px`; унифицировать структурные линии | Каждая линия имеет толщину `1 px` и один цвет, границы не дублируются, цвет не меняется после drag | [x] |
| F22 | Выполнить полный автоматический и визуальный gate | Миграции, unit, integration, browser, build и живой стенд подтверждены | [ ] |

### Этап 7 — восстановление дерева фильтров v5 [IN PROGRESS]

| № | Работа | Условие завершения | Статус |
|---|---|---|---|
| F23 | Вернуть `FilterGroup` и операции узлов в state, API, tools и SQL | Формула `(A И B) ИЛИ (C И НЕ D)` сохраняется и исполняется без упрощения | [x] |
| F24 | Мигрировать v1–v4 без потери v2 `OR/NOT` и сохранить одно поле группировки v4 | Старые представления открываются как v5, `revision` и cursor обновляются | [x] |
| F25 | Сохранить текущую строку чипов и показать вложенную формулу операторами и скобками | Нет отдельного advanced-экрана; текущие панели, размеры и поведение не откатываются | [x] |
| F26 | Выполнить полный gate и визуальную проверку вложенной формулы | Unit, integration, browser, build и живой стенд подтверждены | [ ] |

### Порядок поставки

1. F1–F6: данные и серверная корректность.
2. F7–F11: пользовательский интерфейс.
3. F12–F15: автопагинация и агент.
4. F16–F18: доступность, автоматические проверки и визуальная приёмка.

Коммит этапа разрешён только после его собственных тестов. Трек считается завершённым только после F18; отдельный успешный build или ответ API не закрывает работу.

## Трек DB — Создание исходной базы [COMPLETED]

### Результат трека

```text
db/migrations/                 версионируемая PostgreSQL-схема
db/seeds/                      системные справочники и правила
db/queries/acceptance/         десять эталонных выборок
scripts/db-reset.*             создание чистой базы
scripts/db-generate.*          детерминированная генерация истории
scripts/db-check.*             единая проверка результата
```

Реализованы SQL-миграции, Bash-команды и Python-генератор для локального PostgreSQL 15. Облачная база и публикация данных не требуются.

### DB1 — Окружение и каркас миграций [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB1.1 | Проверить доступные PostgreSQL, `psql` и Docker; выбрать один локальный воспроизводимый запуск | Версия и одна команда запуска записаны в CLAUDE.md |
| DB1.2 | Создать структуру `db/` и `scripts/` без лишнего приложения | Пути соответствуют разделу «Результат трека» |
| DB1.3 | Создать последовательный механизм миграций | Чистая база поднимается одной командой два раза подряд без ручных действий |
| DB1.4 | Добавить команду полного удаления только тестовой базы | Команда требует явного имени тестовой базы и не затрагивает другие базы |

### DB2 — Фермы, животные и справочники [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB2.1 | Создать `farm`, доступ, группы и правила фермы | Ограничения фермы и часового пояса работают |
| DB2.2 | Создать `animal`, родственные связи и базовые атрибуты | Нельзя связать животных разных ферм |
| DB2.3 | Создать справочники статусов, заболеваний, препаратов, процедур, протоколов и единиц | Системные и фермерские значения различаются и версионируются |
| DB2.4 | Настроить tenant-изоляцию | Тестовая роль одной фермы не читает и не изменяет другую |

### DB3 — События и контроль целостности [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB3.1 | Создать `event_type`, общий `animal_event` и типизированные таблицы деталей | Для каждого стандартного типа существует ровно одна допустимая таблица деталей |
| DB3.2 | Реализовать события жизненного цикла, идентификаторов, воспроизводства, продуктивности, измерений, здоровья и протоколов | По одному корректному событию каждого типа записывается транзакционно |
| DB3.3 | Реализовать приплод, заметки, сотрудников, страхование и архив | Состояния открываются и закрываются связанными событиями |
| DB3.4 | Реализовать пользовательские события с типизированными значениями | Неверный тип значения и произвольный бизнес-JSON отклоняются |
| DB3.5 | Реализовать исправление и идемпотентный импорт | Дубль `source_record_id` отклоняется; исправление сохраняет обе версии |

### DB4 — Детерминированный генератор данных [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB4.1 | Задать параметры сценария: фермы, поголовье, период и `seed` | Один набор параметров даёт одинаковый результат при повторном запуске |
| DB4.2 | Сгенерировать карточки, идентификаторы, группы и родство | Нет дублей активных идентификаторов и межферменных связей |
| DB4.3 | Сгенерировать жизненные, репродуктивные и лактационные цепочки | Осеменение, проверка, стельность, сухостой и отёл идут в допустимом порядке |
| DB4.4 | Сгенерировать надои, измерения, болезни, лечение и протоколы | События не выходят за период жизни животного и используют допустимые единицы |
| DB4.5 | Добавить позднюю запись и исправление факта | Различаются фактическая дата и дата знания системы |

### DB5 — Состояние на дату и вычисляемые поля [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB5.1 | Реализовать `animal_state_at(as_of_at, knowledge_at)` | Возвращаются идентификаторы, группа, статусы, лактация, ДДН и стельность |
| DB5.2 | Реализовать ожидаемые даты по версиям правил фермы | Старый расчёт воспроизводится после изменения правила |
| DB5.3 | Реализовать даты событий, интервалы, счётчики, агрегаты, предыдущее значение и логические формулы | Примеры каждого вида имеют проверяемый ожидаемый результат |
| DB5.4 | Реализовать пользовательские поля и псевдонимы | Разные названия одной фермы ссылаются на одно каноническое поле |
| DB5.5 | Создать пересчитываемую текущую проекцию | Проекция полностью удаляется и восстанавливается из исходных событий |

### DB6 — Проверка покрытия [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB6.1 | Зафиксировать десять эталонных SQL-выборок по экранам прототипа | Каждая выборка имеет описание входных условий и ожидаемые животные |
| DB6.2 | Проверить репрезентативные поля из `lists.csv` | Покрыты идентификаторы, группы, статусы, воспроизводство, надои, рост, здоровье, архив и формулы |
| DB6.3 | Проверить временные границы и исправления | Состояние до события, после события и после позднего исправления различается ожидаемо |
| DB6.4 | Проверить изоляцию, ключи, диапазоны, единицы и циклы формул | Каждый запрещённый сценарий завершается явной ошибкой |
| DB6.5 | Проверить план запросов истории | Используются индексы по ферме, животному и времени |

### DB7 — Документация и передача [COMPLETED]

| № | Работа | Условие завершения |
|---|---|---|
| DB7.1 | Синхронизировать DATABASE.md с фактической схемой | В документе нет таблиц и полей, отсутствующих в миграциях |
| DB7.2 | Записать команды создания, генерации, проверки и очистки | Новый запуск выполняется только по CLAUDE.md |
| DB7.3 | Зафиксировать известные расхождения с материалами заказчика | Неподтверждённые формулы, единицы и допущения перечислены явно |
| DB7.4 | Обновить DEVLOG.md и приватный GitHub-бэкап | `main` чистая и совпадает с `origin/main` |

### Условие готовности БД

Единая команда `./scripts/db-check.sh` завершается с кодом `0` и печатает `passed` для миграций с нуля, повторной генерации с тем же `seed`, всех типов событий, межферменной изоляции, временного состояния и поздних исправлений, пользовательских полей, десяти эталонных выборок, запрещённых связей и индексированного запроса истории.

Финальная ручная проверка обязательна: сверить смысл десяти выборок с экранами прототипа и убедиться, что синтетические данные нигде не названы производственными данными заказчика.

## Продуктовый трек тестового задания [IN PROGRESS]

Этот трек относится к содержательной сдаче тестового задания и не дублирует уже выполненную техническую интеграцию Agent 2.

Аналитический этап 1 завершён: пункты 1.1–1.6 воспроизводимы и прошли итоговый QA. Перенос сценария в статический прототип и подготовка отправки остаются следующими отдельными этапами.

| № | Работа | Статус |
|---|---|---|
| 1.1 | Составить ролевые JTBD-сценарии работы Аркаши | [x] |
| 1.2 | Зафиксировать объём, ключи, связи, периоды и SHA-256 четырёх исходных CSV | [x] |
| 1.3 | Разобрать все 2 531 USER-turn и 6 735 списков воспроизводимым пайплайном | [x] |
| 1.4 | Отделить обычных пользователей, интеграторов и сопоставимую когорту 30 компаний | [x] |
| 1.5 | Проверить семантический кодбук на независимых выборках и top-100 списков | [x] |
| 1.6 | Сформировать `docs/project/ANALYSIS.md`, `SOLUTION.md`, `NOTES.md` и машинные результаты | [x] |
| 1.7 | Перенести принятый интерактивный сценарий в `prototype/` и добавить его в `index.html` | [ ] |
| 1.8 | Подготовить архив и ответное письмо | [ ] — нужна отдельная команда |

### Состояние результатов задания

- `SOLUTION.md` — создан: выводы из данных, продуктовая формулировка, поведение и связь с прототипом.
- `docs/project/ANALYSIS.md` и `analysis/` — созданы: метод, полный структурный разбор, независимый semantic QA, машинные таблицы и манифесты.
- `NOTES.md` — создан: разделение решений, воспроизведение, QA и честный список недоделанного.
- Финальный semantic QA: 97 совпадений из 100 при повторной разметке, Cohen's kappa `0,9674`; full-volume эвристика оставлена диагностической из-за непрохождения классовых порогов.
- Сценарий в `prototype/` — ещё не перенесён; рабочая реализация находится в `agent-app/`.

Исходное задание предусматривает передачу папки архивом в ответном письме и последующий созвон на 30 минут. Архивирование и отправка сейчас не разрешены и не выполняются.

## Проверки и ограничения

- 746 локальных ссылок и ресурсов в 48 HTML-страницах и CSS: отсутствующих файлов не обнаружено.
- Стандартный CSV-парсер прочитал все четыре файла; число колонок соответствует заголовкам во всех записях.
- Два внешних JavaScript-файла прошли node --check.
- В браузере пройден путь: оглавление → Команды → «Проверка стельности» → Календарь → «Запуск в сухостой». Таблица, стили и иконки отображаются; ошибок и предупреждений в журнале браузера на этом пути нет. Полная проверка всех кнопок и состояний не выполнялась.
- HTTP-сервер был ограничен 127.0.0.1 и каталогом prototype/; data/ оставался вне раздаваемой папки. После проверки сервер остановлен; команда повторного запуска записана в CLAUDE.md.
- Страницы и используемые стили/скрипт отданы с HTTP 200. Автоматический запрос favicon.ico получил 404: иконки вкладки нет в исходном комплекте, на работу экранов это не повлияло.
- Повторная SHA-256-проверка на этапе подготовки подтвердила: оригиналы не изменены, все 69 копий совпадают; позднее отдельной командой добавлен только DATABASE.md и ссылки на него в служебной документации.
- Содержательная аналитика завершена на всём переданном объёме. Точные показатели структуры и использования рассчитаны по всем строкам; частоты предметных сценариев опубликованы по независимым случайным выборкам с 95% интервалами, потому что автоматическая семантическая классификация не прошла установленный QA-порог.

## Следующий этап

После push документации остановиться. После отдельной команды начать [R00–R02](CSV-RULES-PLAN.md): зафиксировать проверенный baseline, разобрать весь каталог и уточнить карту доработок БД. Не начинать с добавления нескольких популярных фильтров вместо полного объёма. Формальная сдача задания и production-развёртывание остаются отдельными треками.
