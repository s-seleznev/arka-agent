# Журнал разработки

## Очистка распространяемого репозитория

Удалены исходные CSV и описание задания. Аналитика и выгрузки исходных правил исключены из Git без удаления локальных результатов. Сохранены приложение, скиллы, миграции и синтетические генераторы. Локальные БД и настройки облака не изменены. Полная история сохранена в отдельной приватной копии. Публикация аналитики оставлена без изменений по решению владельца.

Проверки: восемь скиллов, TypeScript, production-сборка, доступность локальной БД и проверка состава Git прошли. В сборке остаётся предупреждение о широком трассировании локального Codex runtime; его код этой операцией не изменялся.

Подробности: [состав проекта](docs/project/DISTRIBUTION.md).

# Журнал работы

> История работы. [Навигация и актуальный срез](docs/project/README.md). Датированные разделы описывают свой этап; ранние планы не подтверждают текущую готовность. Пути в командах указаны от корня репозитория.

Связанные документы: [CLAUDE.md](CLAUDE.md) | [ARCHITECTURE.md](docs/project/ARCHITECTURE.md) | [PLAN.md](docs/project/PLAN.md).

## Rules

- Формат времени записи: ДД.ММ.ГГГГ ЧЧ:ММ; перед записью проверить системную дату и время.
- Новые записи добавлять сверху, перед шаблоном.
- После разрешённого git push указать хеш коммита; одна запись — одна рабочая сессия.
- Если коммита или push не было, написать это прямо.
- Каждая запись должна быть понятна без контекста беседы: цель, фактические изменения, проверки и ограничения.
- Отделять действия агента от решений пользователя; фиксировать допущения, ограничения и незавершённую работу здесь. По решению пользователя отдельный NOTES.md удалён.

## 06.09.2026 12:00 — Журнал в корне вместо NOTES

По указанию пользователя DEVLOG.md перенесён из docs/project в корень, NOTES.md удалён. Обновлены относительные ссылки, навигация и дерево документации. Исходное задание клиента сохранено без изменений: упоминания NOTES.md в нём относятся к оригинальным требованиям. Исторические записи журнала также сохранены. Коммит и push для этого переноса не выполнялись.

## 06.09.2026 11:54 — Публичная Арка, OpenRouter и исправление сбоя

**Запрос.** Опубликовать действующий прототип, устранить пустой ответ агента, обновить документацию и отправить изменения.

**Реализация.** Vercel Hobby + Fluid Compute, Neon Free с одной синтетической фермой, OpenRouter `openrouter/free` без платного fallback. Публичный адрес: [arka-agent-demo-2026.vercel.app](https://arka-agent-demo-2026.vercel.app). Приложение называется «Арка», ассистент — «Аркаша». При первом открытии создаются четыре отчёта в собственной гостевой сессии. В срез также входят ранее подготовленные CSV/печать всей выборки, стартовые вопросы, правки UI, скрипты реалистичных имён и разделение входящей/рабочей документации.

**Сбой и исправление.** Vercel прерывал `/api/chat` после 60 секунд. Тест выявил повторяющиеся ошибки аргументов: OpenRouter иногда кодировал фильтры JSON-строкой. Добавлена распаковка со строгой повторной валидацией и без дополнительной генерации. После загрузки скилла сокращён набор инструментов, после применения таблицы оставлен ответ. Максимум функции — 300 с; SDK: весь ответ 240 с, шаг 90 с, пауза потока 60 с, инструмент 30 с. Отключены автоматические сетевые повторы SDK и отдельный запрос модели для названия чата. Ошибка остаётся в чате с кнопкой повтора; повторное сообщение не вставляется в БД дважды. Логи фиксируют этапы и длительности.

**Проверка.** TypeScript и production-сборка прошли. Проверены тайм-аут зависшей модели, распаковка внутри одного вызова SDK, запрет лишних полей и сохранение ограничения фермы. Реальный OpenRouter с облачной БД: 20 животных в группах 4 / 4 / 12, ответ за 43,933 с; вместе с независимой проверкой таблицы — 49,558 с. Предыдущий корректный прогон с лишними попытками: 148,877 / 156,225 с соответственно. Это отдельные измерения, не нагрузочная проверка.

**Публикация и ограничения.** `dpl_HtWw1q5gY8NW2ykx8wdf6md2E3kh` — READY production; публичный alias назначен этой сборке, HTTP 200 и заголовок «Арка» проверены. Прямой автоматический POST блокирует BotID (403); защита не отключалась. Полный браузерный путь после исправления не подтверждён. Не устранено предупреждение Turbopack о широком tracing локального Codex runtime. Ключи и дампы не входят в Git. [Конфигурация и воспроизведение](docs/project/DEPLOYMENT.md).

**Документация.** Обновлены README приложения, обзор, архитектура, описание агента, NOTES и ссылка на прототип в SOLUTION. Аналитические выводы SOLUTION не заполнялись: это отдельная работа.

**Git.** Перед записью `HEAD` и `origin/main` совпали на `3b8723a`; репозиторий приватный. Срез `8ef1de5` успешно отправлен в приватный `origin/main`. Подтверждение push добавлено отдельным документационным коммитом. Локальные `agent-app/.agents/` и `agent-app/skills-lock.json` не включены.

## 06.09.2026 11:40 — Облачный отчёт о коммуникации и активность первого месяца

**Запрос.** Сохранить согласованный отчёт, обновить документацию и отправить изменения в приватный GitHub-репозиторий.

**Решения пользователя.** Два основных блока: возврат пользователей и анализ успешных / неуспешных. Внутри анализа — определение, активность первого месяца, стартовые обращения, задачи успешных и ограничения неуспешных. Прежний восьминедельный критерий сохранён: успешные 39, 58, 80, 224; неуспешных 117. Порог пяти активных дней не принят. Интеграторы переключаются только в возврате и исключены из продуктового анализа.

**Изменения.** Полная таблица 289 пользователей с наблюдением не менее 36 дней; число активных дней и серия ограничены первыми 30 календарными днями с даты первого сообщения. 25 пользователей исключены по наблюдению. Четыре успешных строки зелёные, 117 неуспешных жёлтые, 168 строк без цвета; карточки определений и легенда используют те же цвета. Сохранены недельный график, матрица и исходные данные готовой разметки. Добавлена документация analysis/retention/README.md, обновлены analysis/README.md и NOTES.md.

**Публикация и проверка.** Отчёт работает независимо от локального сервера: https://arka-usage-report.vercel.app/. Vercel deployment dpl_AFDZCHLAHVB4rjmrpbNUgwytmMNu — READY, production. Проверены публичные HTTP 200 и совпадение HTML/JS/CSS с отправленными файлами; в таблице 289 строк, из них 4 зелёные и 117 жёлтых. Проверен порядок двух H2 и пяти H3. Computer Use, повторная семантическая разметка и полная приёмка приложения не запускались.

**Ограничения.** Исходные CSV и полный корпус не публикуются на Vercel. Разрешены только согласованные цитаты, ID и экспорты отчёта. Репозиторий подтверждён приватным; .env*, .vercel/, локальные базы и токены исключены. Частота сообщений не доказывает экономическую ценность или причину активации / ухода; 26 неуспешных пользователей вернулись после восьмой недели. Незавершённый перенос документации и изменения приложения другого агента не входят в этот срез.

**Git.** Срез `d6069eb` успешно отправлен в приватный `origin/main`. Подтверждение публикации записано отдельным документационным коммитом; изменения приложения и незавершённый перенос документации не включены.

## 05.09.2026 23:43 — Сверка актуальности документации

**Запрос.** Проверить документацию после переноса, устранить противоречия и обновить журнал.

**Исправления.** Обновлены README приложения, архитектура, описание базы, требования таблицы и планы: база подключена, ReportView v6, серверная область фермы, четыре верхних действия, нижняя панель 46 px, лимиты CSV/печати, восемь реализованных скиллов. Исторические 51 000 животных отделены от последующих 42 670 записей. Два разных аналитических знаменателя подписаны точно: 338 создателей чатов и 339 авторов USER-сообщений. Исправлены три отложенные ссылки в rules и ссылки на devlog. Старые результаты проверок и утверждения о масштабе больше не выданы за текущие гарантии.

**Проверки.** Подсчёт по исходным CSV выполнен без изменения данных. Структура и локальные ссылки проверены по 62 Markdown-документам, включая новый отчёт. Код и исполняемые скиллы не изменялись; сборка, тесты приложения, БД-миграции и Computer Use не запускались. Внешняя доступность production не проверялась.

**Открыто.** N успешных пользователей и когорта из 300 требуют определения. Полная визуальная и предметная приёмка остаются неподтверждёнными. Подробности: [DOCUMENTATION-AUDIT.md](docs/project/DOCUMENTATION-AUDIT.md). Commit и push не выполнялись.

## 05.09.2026 23:37 — Структура документации

Самостоятельные документы разнесены по происхождению: материалы клиента — docs/incoming, наши документы — docs/project. В корне оставлены SOLUTION.md, NOTES.md и служебные AGENTS.md/CLAUDE.md. Добавлены указатели, обновлены относительные ссылки и дерево проекта. Старые планы отмечены как история; актуальный срез отделён от ранних объёмов данных. В NOTES.md исправлено прежнее утверждение, будто аналитика обосновывает текущий прототип.

Код, исходные CSV, встроенная документация и результаты внутри agent-app, prototype, analysis, rules, db и scripts не переносились. Эти материалы включены в навигацию по существующим путям. Commit и push не выполнялись.

## 05.09.2026 23:21 — Структура финального SOLUTION

По решению пользователя аналитика и прототип описываются как две самостоятельные части: прототип не выдаётся за решение, доказанное поведенческой аналитикой. В SOLUTION.md добавлены дисклеймер, ссылки на аналитические материалы, каркас коротких выводов и описание построения прототипа: база данных → техническое решение → предметные скиллы.

N успешных пользователей из 300 и N скиллов оставлены как placeholders. Численность когорты, критерий успешности, происхождение БД и итоговые выводы требуют заполнения и проверки. Продуктовый код не менялся; commit и push не выполнялись.

## 05.09.2026 19:29 — Восемь отчётов и ускорение агента

**Запрос.** Добиться полезной выдачи по восьми сценариям тестового, сократить технические обращения модели, проверить скорость и опубликовать изменения.

**Изменения.** Уточнены пять процессных скиллов и их маршрутизация. Добавлены 18 отчётных полей из событий и изолированный справочник названий быков/техников. К 150 взрослым коровам добавлены 5 бычков, 10 тёлок и 5 исторических записей выбывших: всего 170 записей на ферме и 42670 в БД. Дополнены события взвешивания, осмотров, надоя и активных протоколов с ответственными. Новая БД не создавалась.

**Ускорение.** configureAnimalTable принимает семантическое дерево фильтров и настройки без UUID. Сервер типизирует значения, удерживает область фермы, проверяет запрос и сохраняет по revision. getFarmSkill возвращает нужные поля и состояние текущего чата. Вложенные И/ИЛИ/НЕ сохранены; старые инструменты доступны для точечных правок и CSV-правил.

**Проверки.** Восемь реальных LLM-прогонов: медиана 46,343 → 18,348 секунды (−60,4%); диапазон 16,948–20,523 секунды, по два вызова, без ошибок и повторов. Повторно проверены состав, колонки, группировки и заполненность всех восьми выдач. Проверки вложенных условий, изоляции, конфликтов revision, типизации и сохранения настроек прошли. Протоколы: analysis/lactis-prime-8/EIGHT-SCENARIOS.md и SEMANTIC-TABLE-SPEED.md, рядом JSON результатов.

**Ограничения.** Одна серия измерений, не p95/SLA и не нагрузочная проверка; данные синтетические. Пересчёт проекции не оптимизировался. Общий TypeScript сохраняет девять прежних TS2322 в прототипных screens; ошибок нового кода не найдено. Computer Use и визуальная приёмка не выполнялись. Локальный стенд отвечает HTTP 307. Дампы и .env.local не публикуются; push переносит код и сценарии, но не содержимое PostgreSQL.

**Публикация.** Срез `3f27ece` успешно отправлен в origin/main. Подтверждение публикации записано отдельным документационным коммитом.

## 05.09.2026 16:48 — Изоляция фермы, 150 коров и группы содержания

**Запрос.** Исправить данные и область доступа агента, затем обновить документы и отправить накопленные изменения.

**Изменения.** Серверная изоляция Лактис Прайм, ограничение CSV-правил и контекста uploads, трассировка инструментов и проверочный ответ updateView. В существующей БД 150 коров с согласованной синтетической историей; остальные пять ферм не изменены, всего 42650 животных. Добавлены шесть секций содержания; первая проверка стельности разделена на 3, 4 и 12 животных. Переключатель «Обзор / Ассистент» меняет меню без перехода. Документы: BACKEND-PROCESSES-PLAN.md, ARCHITECTURE.md, NOTES.md.

**Проверки.** Полная замена сначала проверена с ROLLBACK, затем применена с COMMIT и пересчётом проекции; исходные данные других ферм сверены хешами. Проверка доступа подтвердила одну ферму, 150 строк и отказ для чужого животного, фермы и правила. Группы проверены SQL. Повторный пользовательский запрос: 49,227 секунды, 19 животных, 153 мс суммарно на инструменты; ошибка UUID parentId вызвала повтор updateView.

**Ограничения.** Задержка ответа и неверные UUID остаются открытыми. Данные и секции демонстрационные, без экспертной валидации. Computer Use не применялся; визуальная приёмка не заявляется. Общий TypeScript ранее показывал ошибки string/number в прототипных screens. Дампы БД и локальное окружение не публикуются; push кода не переносит содержимое PostgreSQL.

**Публикация.** Общий срез `5093f3c` успешно отправлен в `origin/main`. Подтверждение записано отдельным документационным коммитом.

## 05.09.2026 12:25 — Визуальные правки чата и таблицы, состояние групп

**Запрос пользователя.** Зафиксировать накопленные изменения интерфейса, обновить документацию и отправить текущую версию.

**Изменения.** Постоянная оболочка чата, возврат к последнему маршруту ассистента, сохранение состояния по чату. Название фермы «Лактис Прайм 8»; управление farmId скрыто в таблице без изменения серверного ограничения данных. Белая правая область, отдельные токены верхней панели и заголовков таблицы, единые разделители и нейтральные обводки. Кнопка таблицы закреплена у правого края экрана. Нижняя панель счётчика имеет высоту 35 px и линию сверху; её слой поднят над прокручиваемыми ячейками. Восстановлены стили ячеек, границы и закреплённый номер. Группы получили понятное название и счётчик в плашке, белый фон, закрепление при прокрутке, раскрытие по умолчанию и локальное сохранение ручного сворачивания. В заголовках колонок добавлено меню скрытия по наведению; номер остаётся обязательным. Уточнены состояния кнопки фильтра и немодальные меню.

**Документация.** Обновлены TABLE-VIEW-REQUIREMENTS.md, ARCHITECTURE.md, NOTES.md. Требования отделены от фактической проверки.

**Проверки и ограничения.** Перед записью проверены дата, git status и origin/main после fetch: расхождения нет. Тесты, линтер, TypeScript и сборка не запускались в рамках визуального ревью. Итоговая браузерная приёмка не выполнена; проверки закрепления и восстановления групп остаются открытыми. Переключатель обзора пока меняет маршрут; требование менять только боковое меню ещё не реализовано.

**Публикация.** Срез `d6b486c` успешно отправлен в `origin/main`. Эта запись о результате публикации добавлена отдельным документационным коммитом.

## 04.09.2026 22:45 — Оболочка Арки, интерфейс ассистента и инструменты таблицы

**Решения пользователя.** Интерфейс ассистента продолжает исходную Арку в светлой теме. Общая боковая панель 248 px, неизменный нижний блок Арки, переключатель «Обзор / Ассистент». «Новая задача» и «Запланировано» — пункты меню; второй пока неактивен. История прокручивается отдельно с затуханием у краёв; названия без вложенного отступа, обычное начертание, даты без разделительных линий.

**Реализация агента.** Перенесены 13 экранов прототипа в React с исходными стилями, логотипом и SVG-иконками. Создана общая оболочка; переключатель плавно меняет ширину и положение выделения, состав меню сменяется через fade out / fade in. Исправлена блокировка клиентского рендера: добавлены границы Suspense, разрешён локальный адрес 127.0.0.1 для dev-ресурсов. Стенд запущен с webpack вместо Turbopack.

Правая область скрыта в новой задаче до открытия пользователем или агентом. Добавлена кнопка панели в шапке чата, плавное появление таблицы и прелоадер под панелью управления. После уточнения пользователя общее состояние между чатами заменено на отдельное состояние каждого чата с сохранением в браузере и восстановлением представления из БД. Первое ручное открытие создаёт выборку без фильтров по всем доступным животным. Общий старый ключ состояния больше не используется.

Таблица и заголовки колонок белые; панель управления, строка фильтров и выпадающие меню серые. Возвращено управление видимостью колонок через существующие операции представления; «Колонки» стоят последними, с иконкой. Фоны hover/selection сделаны непрозрачными, чтобы текст не просвечивал под закреплённым номером.

**LLM и данные.** Настроена gpt-5.6-luna с reasoning medium через Codex App Server. Добавлен инструмент openAnimalTable для открытия таблицы с нуля; инструкции требуют применять инструменты и проверять выдачу, а не обещать действие текстом. Разделены текстовые сообщения модели при сборке потока, добавлены логи начала/завершения вызовов инструментов. Созданы 18 пустых демонстрационных чатов за 6 дат; по отдельной команде удалены 20 чатов «Новый чат». Начальное название новых — «Новая задача».

**Проверки и границы.** В отдельном фоновом браузере подтверждены промежуточные кадры переключателя и fade меню, без управления пользовательскими вкладками. Реальный запрос Luna прошёл getFarmContext → openAnimalTable → getViewState → updateView → queryAnimals, вернул непустую выдачу и событие открытия панели. После перезагрузки компьютера отдельно запущен PostgreSQL; чтение подтвердило сохранность чатов. API фермы ответил 6 фермами и 91 полем. При ошибке истории теперь показывается ошибка с повтором, а не ложный пустой список. Последние атомарные визуальные правки не проходили полную приёмку; визуальная проверка остаётся за пользователем. Постоянный процесс Codex и измерение задержек по этапам пока не реализованы.

В рамках этой команды изменён только DEVLOG.md. Коммит и push не выполнялись: публикацией параллельно занимается другой агент; его изменения не перезаписывались.

## 04.09.2026 22:44 — Общий checkpoint бэкенда и интерфейса

Пользователь поручил обновить документацию и отправить текущую рабочую копию, включая параллельные изменения интерфейса. PLAN.md и BACKEND-PROCESSES-PLAN.md синхронизированы: серверные проверки выполнены; финальную проверку LLM через чат пользователь проведёт в интерфейсе. Полное покрытие частных сценариев не заявляется.

В общий checkpoint включены исследование, восемь скиллов, backend-миграции и тесты, а также текущие UI-изменения параллельного агента. Подробную запись и приёмку интерфейса этот агент оформит отдельно по команде пользователя; этот push сам по себе не означает успешную UI-приёмку. Перед коммитом origin/main обновлён: расхождения с локальной main нет. Общий коммит `8ab357c` (`feat: checkpoint farm process backend and parallel interface work`, 151 файл) отправлен в `origin/main`; отправка подтверждена GitHub. Эта запись фиксирует подтверждение push отдельным документационным коммитом.

## 04.09.2026 22:39 — Проверка backend-пакета основным агентом

По команде пользователя работа продолжена с Luna medium и независимой проверкой основного агента. Найдена и исправлена ошибка пакетного EVENT_COUNT: объединение определений шести ферм умножало счётчики на шесть. Сверка всех 51 000 животных с исходными событиями после исправления дала 0 расхождений. Исправлены границы 7/10 полных дней и порядок событий при одинаковых датах; `028` требует литеральный сдвиг в оптимизированном пути.

Повторный Ovsynch rollback-тест прошёл: действующее назначение, чужое животное, завершение, отмена, исправление и выполненное ИО. Профиль и инструкция воспроизводства версии 2 явно определяют день 10 как старт +9 дней и запрет по статусу DO_NOT_INSEMINATE; bundle обновлён. Восемь контрольных серверных выборок совпали с SQL; чужая ферма недоступна. Это не полное покрытие всех подсценариев восьми инструкций. Результаты и пробелы — в BACKEND-PROCESSES-PLAN.md и analysis/lactis-prime-8/process-queries-smoke.json.

Финальное обновление после `028`: 51 000 животных за 24,696 секунды. Пять unit-проверок скиллов прошли; общий tsc сохраняет 9 ошибок в отдельно разрабатываемых frontend-экранах. Повтор реального LLM-теста отклонён auto-review из-за внешней передачи данных и не обходился. Фронтенд не изменён, commit/push не выполнялись.

Дополнительно основной агент усилил и повторил window-fixture: добавил точную границу 31.08 для 10 суток до 10.09, день за пределами окна, прямой вызов скалярного интерпретатора вместо повторного чтения кеша и строгие NULL-проверки. Средние 15/30 совпали во всех трёх путях. Финальный повтор восьми серверных выборок прошёл. В БД 51 000 животных, фикстуры отсутствуют, проекция свежая, ожидающих блокировки нет.

## 04.09.2026 20:48 — Восемь процессных скиллов в действующем агенте

По команде пользователя обновлён FARM-SKILLS-PLAN.md и реализованы восемь SKILL.md, общий контракт, версия профиля Лактис Прайм 8 и статический bundle. Добавлен getFarmSkill в общий createFarmTools; он доступен через Codex app-server и AI SDK. Системный промпт больше не требует поиска CSV для обычной рабочей задачи; точное применение сохранённых списков сохранено. Лимит AI SDK увеличен с 5 до 8 шагов для чтения инструкции, контекста, вида, обновления, запроса и ответа. Для нового инструмента добавлена подпись состояния в чате, без редизайна.

Luna medium проверил условия и отметил риск переноса профиля на чужие фермы. Добавлена явная конфигурация профиля и allowlist доступных UUID: чужая, пустая или ненастроенная область отклоняется. В .env.local сохранены шесть ферм текущего стенда; company_id=8 не служит доступом или farmId. Авторизация не менялась. Принятые допущения процесса хранятся в profile.json, не выводятся как повторяющиеся дисклеймеры пользователю.

Пять unit-проверок и проверка bundle прошли. Реальная LLM через Codex app-server загрузила youngstock, сформировала фильтр последнего веса 500–600 кг, сохранила ферму и получила 681 животное. Независимый SQL подтвердил 681; searchListRules не вызывался. Временные чат и вид удалены. Next поднят на 127.0.0.1:3000. Протокол — analysis/lactis-prime-8/skill-smoke.json.

Общий tsc остаётся с девятью ошибками TS2322 в неизменённых этим этапом экранах components/arka/screens; новых ошибок после исправления теста нет. Полная production-сборка, GUI-приёмка и исполнение всех восьми процессов не заявлены. Отсутствующие поля БД не создавались и не подменялись. Источники, факты БД и старая аналитика сохранены. Commit и push в этой сессии не выполнялись.

## 04.09.2026 20:23 — Исследование процессов Лактис Прайм 8

По команде пользователя выполнен исследовательский план, без изменений приложения и БД. Результаты находятся в `analysis/lactis-prime-8/README.md`: срезы с исходными ID и контрольными суммами, каталог, варианты с полными фильтрами/колонками, области применения, переписки, вопросы специалисту и протокол проверки.

Учтены 259 списков (106 с запусками, 153 без) и 28 чатов. Все 47 USER-сообщений прочитаны: 28 от трёх пользователей, 19 от двух интеграторов. Девять сообщений содержат прямой запрос на выборку/подсчёт животных, из них шесть пользовательских. AI использован как ограниченный контекст, не источник правил; полный аудит ответов не заявляется.

Выделены 24 аналитических семейства и отдельный технический раздел — не 24 команды или скилла. Показаны семь повторяемых форм запроса; варианты условий сохранены. Не найдено явное правило дня осеменения коров после Овсинх; обнаружены расхождения порогов и названий, различия правил секций, неизвестные формулы. Подготовлены адресные вопросы специалисту.

Один проверяющий Luna medium независимо проверил структурное покрытие и выбранные содержательные выводы. По замечанию диагностический отбор на бруцеллёз отделён от вакцинаций; границы проверки и разбор замечаний сохранены в review.md/resolution.md. Профессиональная валидация не проводилась.

Проверки: исходные SHA-256 неизменны; покрытие 259/47/28, сумма 3 982 запуска и 106 использовавшихся списков сверены; повторная генерация восьми артефактов побайтно стабильна; ссылки и git diff --check прошли. План и обзор подхода обновлены. Commit и push не выполнялись; приложение, БД и старая аналитика не менялись. Дальнейшая разработка ожидает решения пользователя.

## 04.09.2026 20:10 — План исследования Лактис Прайм 8

По команде пользователя подготовлен `docs/project/LACTIS-RESEARCH-PLAN.md`, обновлён указатель текущего этапа в `docs/project/PLAN.md`. Цель — восстановить процессы одной компании из 259 списков и 28 чатов, разделить общие команды, варианты правил и области применения, оценить достаточность материала для инструкций агенту.

Порядок: срез данных → повторения → 106 использовавшихся списков → оставшиеся 153 → разбор диалогов и ролей → каталог с исходными ID → независимая проверка → выводы и вопросы специалисту. Ресурсы: Astra medium и один проверяющий Luna medium после подготовки каталога. Критерии полноты и границы зафиксированы в плане.

Проверка: план сверён с обсуждённым объёмом и ограничениями существующего анализа; код, БД и исходники не менялись. Само исследование и субагенты не запущены. Commit и push не выполнялись в рамках этой команды.

## 04.09.2026 19:45 — Фиксация версии и пауза перед выбором подхода

По команде пользователя обновлена документация без изменения кода и БД. Создан `docs/project/LISTS-APPROACH.md`; в `docs/project/CSV-RULES.md` и `docs/project/CSV-RULES-PLAN.md` обозначена пауза в расширении каталога. Сохранён технический checkpoint `f717cd9` / `88fd6db` и его ограничения: 882 исполнимых определения из 6 735, без заявления полного покрытия или профессиональной валидации.

Зафиксированы значение исходных списков, пример объединения определений 264–267 в команду с параметрами и содержательные различия 305/306. Подход со скиллами рассматривается как вариант, а не принятое решение. Дальнейшая реализация ожидает отдельной команды пользователя.

Проверка: исходные вводные и определения CSV прочитаны; удалённая main перед изменениями совпадала с HEAD. Публикуется только эта фиксация документации; прежние незакоммиченные изменения аналитики не включаются. Хеш публикации сообщается после проверки push.

## 04.09.2026 18:55 — Исполнение правил CSV на существующем стенде

### Команда и границы

Пользователь разрешил реализацию R00–R21 и работу команды Astra medium. Сохраняются текущий UI, подписка ChatGPT/Codex, шесть ферм и 51 000 животных. Расписания, запись фактов моделью и production-деплой не выполняются.

### Реализовано

Каталог всех 6 735 исходных определений хранится в продуктовой БД с неизменяемыми версиями источника, parser и bindings. Общие инструменты поиска, чтения и применения правила работают через оба runtime. Перед сохранением выполняются проверка доступа, полная компиляция, SQL-запрос и проверка revision; ошибка не меняет таблицу частично. `ReportView v6` сохраняет происхождение, точные десятичные числа, ссылки на поля, относительные даты и дерево И/ИЛИ/НЕ. Ручные правки и обновление среза сохраняют настройки.

В фермерской БД введён общий реестр вычисляемых/custom-полей с типами, версиями, временными границами, исправлениями событий и пакетной проекцией. Реестр проходит через API, compiler, SQL и UI. Подключаются проверенные существующие факты рождения, выбытия, контрольной дойки, осеменений и взвешиваний; неясные исходные названия не подменяются похожими полями.

### Приёмка и открытые критерии

Итоговый SQL-аудит: 882/882 исполнимых определения, 0 ошибок; 5 853 заблокированных определения не объявлены выполненными. p50 103,8 мс, p95 128,22 мс, максимум 318,6 мс для запроса вместе со сводкой. Фактическое покрытие, проверки и незакрытые R00–R21 находятся в [rules/implementation-status.md](rules/implementation-status.md); полный план не объявлен завершённым. Исходные 18 ячеек каждой записи сохранены, parser v2 разобрал 6 702 записи и оставил 33 точные диагностики. Backup обеих БД реально восстановлены в отдельных копиях. Проверки LLM включают успешный список 541 и три уточнения неоднозначного запроса; предыдущие ошибки также сохранены в протоколе.

Отдельная production-сборка обнаружила серверный `node:crypto` в общем коде таблицы; он заменён стандартным `crypto.randomUUID()`. Найден и исправлен конфликт трактовки пустой OR-группы между SQL и проверкой области фермы. Новые поля сначала вызвали пересчёт дольше runtime timeout. Миграция 019 сократила применённый полный пересчёт до 14,486 секунды при неизменном ограничении 60 секунд. Полный поиск 9 504/9 504 прошёл после исправления 15 ошибок числовых названий и крайних пробелов.

Auto-review отклонил финальный UI-refresh собственного демонстрационного чата 541; его прежняя трасса не считается свежей проверкой после новых миграций. Факты животных при этом не менялись.

После вопроса пользователя о расходе лимита расширение объёма остановлено: завершается текущий технический сбой и checkpoint. Для дальнейших однотипных задач пользователь выбрал Luna; оценка следующей партии должна предшествовать её запуску.

### Публикация

Baseline дерева ранее опубликован как `2deba93`. Реализация опубликована в приватном `origin/main`: `f717cd9`. Удалённая ветка проверена после push; эта отметка публикуется отдельным служебным коммитом. Прежние аналитические изменения оставляются вне поставки.

## 04.09.2026 17:19 — План исполнения правил CSV и подготовки БД

### Решение пользователя

Подготовить подробный план инфраструктуры для правил CSV и нужных им данных, обновить документацию и журнал, выполнить push. После этого остановиться до отдельной команды на реализацию.

### Выполнено

Созданы [CSV-RULES.md](docs/project/CSV-RULES.md) и [CSV-RULES-PLAN.md](docs/project/CSV-RULES-PLAN.md): полный каталог источника, версионирование, поля и параметры, безопасный разбор и исполнение, подготовка фактов/формул, выбор правила агентом, совместимость таблицы и доказательство покрытия. План содержит 22 задачи R00–R21 с зависимостями и критериями приёмки; все задачи реализации оставлены незапущенными.

Обновлены архитектура, спецификация БД, интеграционный документ, основной план и указатели CLAUDE.md. Зафиксированы сохранение существующих 6 ферм и 51 000 животных, отсутствие работ по расписаниям и редизайну, отдельная проверка прежних локальных изменений v5. Различаются точность условий и происхождение вычисляемых/тестовых значений.

### Проверки и найденные ограничения

CSV прочитан целиком: 6 735 записей, 30 компаний, уникальные пары company/list; 6 001 обычный и 734 календарных списка. По метаданным максимум 119 фильтров и 43 колонки; действующие пределы 30/30 недостаточны. SHA-256 источника записан в спецификации, файл не изменён.

Чтением БД подтверждены 51 000 животных, 14 определений полей и 8 формул. Чтением кода найдено расхождение грамматик seed-формул и исполнителя, отсутствие автоматического вычисления произвольного зарегистрированного поля и инструментов поиска правил CSV. Эти разрывы включены в план, а не помечены устранёнными.

Проверены ссылки, структура Markdown, уникальность всех 22 ID задач, отсутствие ошибок diff и состав коммита. Исходники приложения сохранили контрольные суммы на начало этой работы. Миграции, генератор, код, UI и тестовые запуски приложения не выполнялись; прежние локальные изменения кода и аналитики сохранены вне коммита.

### Push и остановка

Документация опубликована в приватном `origin/main` коммитом `a4d8586`; эта запись журнала публикуется отдельным служебным коммитом. Реализация R00–R21 ожидает отмашки пользователя.

## 04.09.2026 15:29 — Одноуровневая группировка и быстрый режим UI

### Цель

Упростить группировку до одного атрибута, выровнять геометрию оболочки и перейти к быстрым атомарным UI-правкам во время визуального ревью.

### Решения пользователя

В таблице допускается только один уровень группировки. Верхние панели и сложенное боковое меню имеют размер `46 px`. Все структурные линии имеют толщину `1 px` и один нейтральный цвет. Во время серии мелких правок результат сразу показывается через hot reload без тестов и сопутствующей работы; полная приёмка запускается только отдельной командой.

### Выполнено

Введён `ReportView v4`: UI, agent tools, API и БД принимают не более одного правила `groupBy`. Миграция оставляет внешний уровень старых представлений, а отброшенные поля переносит в колонки. Добавлено ограничение БД; 14 прежних многоуровневых представлений преобразованы без потери полей.

Каркас выровнен по `46 px`. Разделители бокового меню, чата, правой панели и таблицы используют один токен; удалены двойные границы, цветное состояние после drag и утолщённая тень закреплённой колонки. Режим быстрых UI-правок записан в `AGENTS.md`; требования, архитектура интерфейса и план обновлены.

### Проверки

До перехода в быстрый режим прошли unit 14/14, integration 5/5, `db:check`, TypeScript и целевой browser-набор геометрии 7/7. Полный browser-прогон дал 45/47: две гонки тестов исправлены, повторный полный прогон не запускался по принятому правилу быстрых UI-правок.

### Push

Реализация и документация опубликованы коммитом `7532f1d`; эта запись журнала публикуется следующим служебным коммитом.

## 04.09.2026 11:35 — Table View v3 и завершение интерфейса стенда

### Цель

Заменить перегруженный Table View v2 принятым плоским интерфейсом фильтров, исправить оболочку чата и боковое меню, сделать ферму обычным фильтром и подтвердить работу пользователя и агента на базе из 51 000 животных.

### Решения пользователя

В верхней строке остаются только **Фильтр**, **Сортировка** и **Группировка**. Кнопка **Фильтр** только показывает строку активных чипов; новый фильтр создаётся из этой строки. Расширенный фильтр удалён: между условиями действует **И**, внутри множественного значения — **ИЛИ**, также доступны отрицательные операторы и диапазон. Ферма — съёмный рядовой фильтр; без него видны все разрешённые фермы.

В шапке чата остаётся только редактируемое название. Удалены название Аркаши, управление доступом, логотип бокового меню и массовое удаление чатов. Номер животного закреплён первой колонкой и открывает стандартный `Sheet` по клику на текст или отдельную кнопку, появляющуюся при наведении на ячейку.

### Выполнено

Введён `ReportView v3`, миграция v1/v2, типизированные плоские операции и единый контракт для интерфейса и агента. Multi-farm RLS читает все фермы из `farm_access`; курсор связан с разрешённым набором ферм. Исправлены проекция обязательных колонок, конфликты revision, запоздалые ответы при переключении чатов и независимые состояния загрузки разных чатов.

Пересобраны фильтры, сортировка, группировка, боковое меню, шапка, фиксированная колонка номера, карточка животного, полноэкранные разделители и независимое сохранение ширины панелей. Цвета, шрифты, скругления и интерактивные состояния привязаны к токенам переданного прототипа.

### Проверки

Unit: 13/13; integration: 5/5; browser: 23/23. TypeScript, Ultracite по 171 файлу, миграции, `db:check`, production build и 24 маршрута прошли. На 6 фермах по 8 500 животных benchmark за 40 итераций: p50 1,1 мс, p95 1,9 мс.

Визуальная проверка при 1440×900 не выявила ошибок консоли, перекрытий и самопроизвольно открытых меню. Сквозная проверка через текущую ChatGPT-сессию и модель `arkasha` изменила выдачу с 8 500 до 1 020 животных, увеличила revision с 0 до 1 и подтвердила сохранение фильтра после перезагрузки.

### Push

Реализация зафиксирована коммитом `9641491`; эта запись журнала публикуется следующим служебным коммитом.

## 03.09.2026 23:13 — Реализация Table View v2

### Цель

Реализовать принятый Notion-подобный интерфейс фильтрации, сортировки и группировки с единой моделью операций для пользователя и агента, серверной выборкой и корректной работой на 51 000 животных.

### Выполнено

Введены `ReportView v2`, стабильные ID и вложенный `AND/OR/NOT`; добавлены типизированные редакторы, простая и расширенная настройка, атомарный Apply/Cancel, сортировка и группировка с приоритетами. Фильтрация и группы перенесены на сервер; cursor связан с revision, запросом, проекцией и группой. Добавлены автопагинация, retry, viewport-контекст, rebase независимых операций и явные конфликты. Номер животного открывает стандартный `Sheet`; выделение ограничено 200 строками.

### Проверки

Unit: 13/13; integration: 4/4; browser: 13/13. TypeScript, Ultracite, production build, миграции, RLS и 10 SQL-сценариев пройдены. На базе из 6 ферм × 8 500 животных серверный benchmark за 40 итераций: p50 1,0 мс, p95 1,2 мс. Интерфейс проверен визуально на работающем стенде.

### Push

`87103d8 Implement Table View v2`; отправлен в `origin/main`. Эта запись журнала отправляется следующим служебным коммитом.

### Решения и ограничения

F1–F18 закрыты. Следующий технический этап — production Neon, облачная API-модель и сквозная проверка развёрнутого стенда.

## 03.09.2026 21:33 — Требования и план представления таблицы

### Цель

Превратить исследование Notion-подобной панели в исполнимые требования и поэтапный план: фильтр, сортировка, группировка, полная логика `AND/OR/NOT`, серверные группы, автопагинация и точный контекст таблицы для агента.

### План

Зафиксировать модель `ReportView v2`, операции UI и агента, серверные и клиентские контракты, доступность, производительность и приёмку; затем разложить реализацию по зависимостям и связать новые документы с основной документацией проекта.

### Выполнено

Создан `docs/project/TABLE-VIEW-REQUIREMENTS.md`: границы этапа, неизменяемые правила, v2-модель, миграция, typed operations, функциональные и API-требования, серверная группировка, snapshot cursor, автопагинация, viewport, доступность, производительность и 16 сценариев приёмки. В `docs/project/PLAN.md` добавлен трек F0–F18 с порядком поставки и условиями завершения каждого шага. `docs/project/FILTER-UX.md`, `docs/project/AGENT-DATA-UI.md`, `docs/project/AGENT.md`, `docs/project/ARCHITECTURE.md` и `CLAUDE.md` связаны с канонической спецификацией; в архитектуре исправлено описание двух model runtime и фактической передачи загруженной страницы вместо точного viewport.

### Отклонения

Реализация F1–F18 не начиналась. Текущий конструктор, offset cursor, клиентская группировка страницы и неточный `visibleRowIds` остаются baseline, явно отделённым от целевого состояния.

### Проверки

`git diff --check` пройден; все относительные ссылки семи изменённых документов разрешаются. Требования сверены с текущими лимитами 50/200 строк, 30 условий, 5 сортировок и 3 группировок. Код приложения не менялся, поэтому build и browser-тесты не запускались.

### Push

`f407df2 Document table view requirements and delivery plan`; отправлен в приватный `origin/main`. Эта запись журнала отправляется следующим служебным коммитом.

### Решения и ограничения

Полная логика определяется рекурсивной грамматикой `AND/OR/NOT` в документированных ресурсных пределах. Фильтрация, сортировка и группировка выполняются по полной выборке на сервере; cursor связывается с revision, fingerprint и snapshot данных. Автозагрузка не заменяет доступную с клавиатуры кнопку **Загрузить ещё**. Агент получает только сохранённый view, фактический viewport, раскрытые группы и выделение.

## 03.09.2026 21:22 — Полный анализ данных тестового задания

### Цель

Полностью выполнить аналитическую часть тестового задания: изучить реальные чаты, пользователей и 6 735 списков, определить востребованные сценарии работы Аркаши со списками и подтвердить выводы воспроизводимыми цифрами.

### План

Параллельно начать три потока: проверку качества и связности четырёх CSV, содержательную классификацию всех 1 390 чатов и структурный разбор всех 6 735 списков. Затем объединить результаты в единую матрицу сценариев, проверить неопределённые случаи и подготовить воспроизводимый скрипт, методику, машинные результаты, аналитический отчёт, `SOLUTION.md` и `NOTES.md`.

### Выполнено

Обработаны все четыре исходных CSV: 5 022 сообщения, 1 390 чатов, 339 профилей и 6 735 списков. Проверены структура, ключи, связи, пропуски, периоды, компании и SHA-256; выделены 2 531 USER-turn, 338 авторов, интеграторы и сопоставимая когорта 30 компаний. По всем строкам построены диагностические классификации, а прямой спрос на списочную выдачу дополнительно посчитан консервативной взаимоисключающей воронкой на уровне всех чатов.

Созданы версионированный кодбук, воспроизводимые скрипты, построчные результаты, манифесты и слепые выборки. Независимо размечены 334 USER-turn, 364 случайных списка, все 100 наиболее запускаемых списков и повторная выборка 50 + 50. После аудита первая разметка отброшена: раскрывающие предсказание страты удалены, контекст строк расширен до полных исходных полей, весь semantic QA выполнен заново.

Обновлены `docs/project/ANALYSIS.md`, `SOLUTION.md`, `docs/project/AGENT-JTBD.md`, `NOTES.md`, `docs/project/PLAN.md`, `analysis/README.md` и `analysis/REVIEW-PROTOCOL.md`. Итоговая матрица выбрала для проверки прототипом запуск и сухостой, охоту и осеменение, лечение и профилактику, молодняк, больных животных и группы риска. Завершено 03.09.2026 в 23:02.

### Отклонения

Автоматический full-volume классификатор не прошёл классовый QA: agreement с независимой разметкой составил 54,49% для USER-turn и 70,05% для списков. Его сценарные числа оставлены диагностическими; опубликованные частоты взяты из случайных слепых выборок с 95% интервалами Wilson. Между чатами 06.10.2025–31.01.2026 и снимком списков за август 2026 нет построчной связи, поэтому причинная цепочка «запрос → созданный список» не заявляется. Человеческой или доменной валидации меток в переданных материалах нет.

### Проверки

`python3 analysis/analyze.py --check`, `python3 analysis/chat_list_funnel.py --check`, `python3 analysis/merge_qa.py` и компиляция трёх Python-скриптов завершились с кодом `0`. Повторный полный запуск не изменил SHA-256 ни одного из 31 машинного результата и review-файла. Отдельная сверка подтвердила исходные и выходные количества, 792 безопасные слепые строки без предсказаний, 898 review-записей, одинаковые метки шести пересечений global/top-100, 20 уникальных рангов и отсутствие битых локальных ссылок. Повторная разметка совпала в 97 из 100 случаев; Cohen's kappa `0,9674` при пороге `0,80`. `git diff --check` прошёл.

### Push

Commit и push не выполнялись: отдельной команды на публикацию аналитического этапа не было.

## 03.09.2026 21:19 — Интеграция агента, базы фермы и правой таблицы

### Цель

Связать чат-продукт Аркаши с исходной базой фермы и интерактивной таблицей справа, чтобы пользователь и агент работали с одним состоянием выборки животных.

### План

Подключить фермерскую базу отдельным серверным соединением, ограничить доступ активной фермой, добавить сохранённые представления и типизированные tools, передавать состояние таблицы в чат и проверить решение на 51 000 животных. Отдельно описать сценарии Аркаши и замену непонятной панели фильтров на Notion-подобный интерфейс.

### Выполнено

Добавлены `FARM_DATABASE_URL`, проверка `farm_access`, индексированная проекция `animal_state_query`, API ферм, животных и `ReportView` с optimistic locking по `revision`. Агент получил типизированные tools чтения фермы, выборки животных, состояния представления, изменения таблицы и открытия карточки животного; произвольный SQL модели недоступен. Правая панель показывает реальную таблицу животных, передаёт агенту ферму, фильтры, сортировку, группировку, выделение и ID загруженной страницы, а изменения агента применяет к тому же `ReportView`.

Локальный LLM-runtime подключён через официальный `codex app-server` и текущую подписку ChatGPT; авторизация хранится вне проекта в `~/.codex` и переживает перезапуски приложения. Генератор расширен до 6 ферм по 8 500 животных. Созданы спецификация интеграции `docs/project/AGENT-DATA-UI.md`, каталог 20 JTBD-сценариев `docs/project/AGENT-JTBD.md` и целевой UX-контракт `docs/project/FILTER-UX.md`.

### Отклонения

Текущий конструктор фильтров не принят: создание и применённое состояние смешаны, нет полного дерева `AND/OR/NOT`. Текущая группировка выполняется только внутри загруженной страницы и поэтому некорректна для большой выборки. Их замена описана в `docs/project/FILTER-UX.md`, но ещё не реализована. Production Neon, облачный API-провайдер модели и новый deployment также не выполнены; локальная авторизация ChatGPT недоступна Vercel runtime.

### Проверки

Зафиксирован набор из 51 000 животных: 6 ферм × 8 500. Прямые SQL count и первая страница на 40 итерациях после 5 прогревочных показали p50 0,6 мс и p95 0,9 мс; HTTP и React этим замером не охвачены. Проверки базы, RLS и десяти эталонных выборок пройдены; штатная браузерная автоматизация на mock-модели — 29 из 29. Отдельный ручной smoke подтвердил ответ живого Codex app-server и вызов `updateView` со сменой фермы. Форматирование, TypeScript и production build завершились успешно.

### Push

`36edfff Integrate Arkasha farm agent and document filter UX`; отправлен в приватный `origin/main`. Эта финальная запись статуса отправляется следующим служебным коммитом.

### Решения и ограничения

Факты фермы остаются в `arka_test`, а продуктовая база хранит чаты, файлы и состояния представлений. Основная ссылка на животное в таблице — его номер; карточка открывается из номера в правой выдвижной панели. Следующий этап — реализовать `docs/project/FILTER-UX.md`, затем подключить production Neon и облачную модель и проверить развёрнутый стенд.

## 02.09.2026 23:30 — Полный чат-продукт Аркаши

### Цель

Заменить минимальный Eve-стенд полноценным продуктом на актуальной инфраструктуре Vercel: история и управление чатами, документы, фотографии, база знаний и постоянная правая область просмотра.

### План

Взять официальный `vercel/ai-chatbot`, сохранить отдельный Vercel project, добавить private Blob, чатовую PostgreSQL-базу, прямое подключение нашей модели и каркас будущих списков фермы.

### Выполнено

`agent-app/` полностью переведён на Next.js 16.2.10 и AI SDK 7.0.15. Добавлены гостевые и постоянные аккаунты, URL и история чатов, создание, переключение, переименование и удаление, документы и фотографии до 20 МБ, private Vercel Blob, извлечение текста PDF, DOCX, XLSX, TXT, Markdown, CSV и JSON, поиск по загруженным документам, передача изображений модели и приватная выдача файлов. Справа реализованы предпросмотр файлов и независимые состояния `file | list | table` для следующей интеграции с базой фермы. Интерфейс Аркаши локализован на русский. Локальная чатовая база `arkasha_chat` отделена от исходной `arka_test`; `AUTH_SECRET` и Blob настроены в Vercel.

### Отклонения

Production-версия не опубликована: владелец Vercel-команды должен принять условия Neon Marketplace. Реальный ответ нашей модели не проверен, потому что отсутствуют base URL, model ID и credential.

### Проверки

`corepack pnpm run check`, `corepack pnpm exec tsc --noEmit` и `corepack pnpm run build` завершились успешно. Playwright-сценарий загрузил Markdown-файл, подтвердил запись метаданных, приватный API и предпросмотр справа: `1 passed`. Финальный двухпанельный интерфейс проверен в браузере.

### Push

`39ec8f6 Build full Arkasha chat product`; отправлен в приватный `origin/main`. Эта запись журнала отправляется следующим служебным коммитом.

### Решения и ограничения

Подключение `arka_test`, выборки животных и визуализация списков перенесены на следующий этап по решению пользователя. Код готов принимать OpenAI-compatible модель через `AI_PROVIDER_BASE_URL`, `AI_PROVIDER_API_KEY` и `AI_MODEL_ID` без зависимости от Vercel AI Gateway.

## 02.09.2026 21:20 — Закрытый Eve Web Chat

### Цель

Выполнить согласованный план: добавить к Eve backend веб-интерфейс, закрытый доступ и production deployment, затем подключить нашу модель.

### План

Добавить официальный `channel/web`, проверить интерфейс и session flow, настроить доступ и развернуть в существующий Vercel project.

### Выполнено

Командой `eve add channel/web` добавлены Next.js 16.3.4, AI Elements, `withEve` и `useEveAgent`. Чат показывает сообщения, loading и error state, создаёт session URL и позволяет начать новый диалог. `placeholderAuth()` заменён на runtime principal, который допускает запросы только через защищённые Vercel Authentication host. Стенд опубликован на [arkasha-agent-<vercel-scope>.vercel.app](https://arkasha-agent-<vercel-scope>.vercel.app).

### Отклонения

Наша модель не подключена: в окружении нет provider credential, model ID и API type. Дефолтный AI Gateway доходит до модельного вызова, но Vercel отклоняет его без способа оплаты.

### Проверки

`npm run typecheck`, production `npm run build` и Vercel deployment завершились с кодом `0`; `npm audit` — ноль уязвимостей. Локальный и production чат проверены в браузере на desktop и mobile: сессия создаётся, URL меняется, ошибка показана, поле ввода остаётся рабочим. Production health возвращает `200`, публичный session API — `401`.

### Push

`59b2166 Deploy secure Eve web chat`; отправлен в приватный `origin/main`. Эта запись статуса отправляется следующим служебным коммитом.

### Решения и ограничения

Для закрытого доступа используется стабильный branch URL, потому что Hobby-план Vercel не защищает production alias. Для завершения Agent 1 нужны provider/API type, model ID и credential нашей модели.

## 02.09.2026 20:49 — Перепроектирование целевого стенда агента

### Цель

Исправить границу результата: пользователю нужен бот с веб-интерфейсом и нашей моделью, а не только backend-каркас Eve с моделью Vercel AI Gateway.

### План

Зафиксировать целевую архитектуру и критерии готовности, сохранить полезный deployment как основу, разделить дальнейшую работу на подключение модели, Web Chat, доступ и production-проверку.

### Выполнено

Создан AGENT.md с целевым результатом, архитектурой, контрактом модели, требованиями к интерфейсу, доступу и приёмке. PLAN.md переписан: предыдущий deployment отмечен как ограниченный backend-каркас, добавлен новый исполнимый трек веб-бота. CLAUDE.md и ARCHITECTURE.md связаны с новой спецификацией.

### Отклонения

Код не изменялся. Реализация не начата, потому что команда пользователя ограничена документацией и планом.

### Проверки

Проверена официальная документация установленного `eve 0.47.3`: для существующего проекта Web Chat добавляется через `eve add channel/web`, использует Next.js, `withEve` и `useEveAgent`, а `placeholderAuth()` должен быть заменён перед браузерным production-доступом.

### Push

`42b40b2 Plan web agent with custom model`; отправлен в приватный `origin/main`. Финальная запись статуса отправляется следующим служебным коммитом.

### Решения и ограничения

Наша модель должна подключаться server-side и не зависеть от Vercel AI Gateway, если пользователь отдельно не выберет Gateway. До начала реализации нужны provider/API type, model ID, credential и способ закрытого доступа к стенду. Интеграция с PostgreSQL остаётся следующим этапом.

## 02.09.2026 20:41 — Базовый агент Vercel

### Цель

Развернуть официальный дефолтный агент Vercel отдельным стендом без предметных инструкций и интеграции с базой данных.

### План

Создать приложение через `eve init`, проверить локальную сборку, привязать отдельный Vercel project, выполнить production deployment и сквозной вызов модели.

### Выполнено

Добавлен `agent-app/` на `eve 0.47.3` и AI SDK 7 с дефолтной инструкцией, моделью `openai/gpt-5.6-luna-fast` и стандартным HTTP-каналом. Создан Vercel project `arkasha-agent`, production deployment опубликован на [arkasha-agent.vercel.app](https://arkasha-agent.vercel.app), OIDC получен через `eve link`. Архитектура, команды и план синхронизированы с фактическим стендом.

### Отклонения

Последняя npm-версия `eve 0.49.1` не прошла локальное правило минимального возраста релиза; установлен последний разрешённый релиз `0.47.3`. Production runtime работает, но AI Gateway не выполняет модельные запросы, пока в аккаунте Vercel не добавлен способ оплаты.

### Проверки

`npm run typecheck` и `npm run build` завершились успешно; `npm install` сообщил о нуле уязвимостей. Локальный и production `/eve/v1/health` вернули `200` и статус `ready`. Удалённый TUI подключился к production и дошёл до вызова модели, который завершился точной ошибкой Vercel о требуемом способе оплаты.

### Push

`3528b31 Deploy default Vercel Eve agent` и `ad05761 Document Vercel agent deployment`; отправлены в приватный `origin/main`.

### Решения и ограничения

Пользователь поручил развернуть дефолтного агента и отдельно подтвердил вход в Vercel через Google. Интеграция с PostgreSQL не выполнялась. После добавления способа оплаты остаётся повторить один сквозной модельный вызов; изменений кода для этого не требуется.

## 02.09.2026 19:17 — Реализация исходной событийной базы

### Цель

Реализовать согласованную исходную базу фермы без Аркаши и продуктового интерфейса.

### План

Выполнить этапы DB1–DB7: локальный PostgreSQL, миграции, типизированные события, генератор истории, временные срезы, формулы, безопасность, эталонные выборки и документацию.

### Выполнено

Добавлены семь миграций PostgreSQL, системные справочники, 33 типа событий, типизированные пользовательские поля, версионируемые правила и формулы, RLS, ограничения целостности и неизменяемая история исправлений. Детерминированный генератор создаёт две тестовые фермы и 80 животных; все записи явно помечены `GENERATED TEST DATA`. Добавлены десять SQL-выборок по примерам прототипа и единая команда `./scripts/db-check.sh`. CLAUDE.md, ARCHITECTURE.md, DATABASE.md и PLAN.md синхронизированы с реализацией.

### Отклонения

Docker не установлен и не потребовался: использован существующий Homebrew PostgreSQL 15. Unix-сокет перенесён в `/tmp/arka-head-product-pg`, потому что полный путь внутри проекта превышает лимит PostgreSQL в 103 байта. Исходные CSV не импортировались.

### Проверки

`./scripts/db-check.sh` завершился с кодом `0`: повторяемость генератора, все типы событий, временное исправление, формулы, запреты межферменных и неверно типизированных данных, десять эталонных выборок, RLS и использование индекса истории получили `passed`. Дополнительно пройдены `bash -n`, `python3 -m py_compile` и `git diff --check`.

### Push

`7e91d32 Implement source farm database` и `7a73b9d Ignore generated Python cache`; отправлены в приватный `origin/main`.

### Решения и ограничения

Пользователь поручил начать реализацию базы. Агент сохранил её отдельным слоем исходных данных: прототип, Аркаша и четыре выгрузки заказчика не изменялись. Производственные единицы, формулы и правила доступа по-прежнему требуют подтверждения на реальной схеме заказчика.

## 02.09.2026 18:48 — План создания исходной базы

### Цель

Подготовить исполнимый план разработки базы по согласованной событийной модели и привести журнал к фактическому состоянию проекта.

### План

Разделить реализацию на проверяемые этапы от локального PostgreSQL и миграций до генератора истории, расчёта состояния на дату и эталонных выборок.

### Выполнено

В PLAN.md добавлен отдельный трек DB из семи этапов: окружение, основные таблицы, события, генератор, вычисления, проверка покрытия и передача. Для каждой работы задан наблюдаемый результат. CLAUDE.md обновлён: разрешены спецификация и план базы, реализация ещё не начата.

### Отклонения

Реализация базы не выполнялась: команда пользователя ограничена подготовкой плана.

### Проверки

Проверены Markdown-ссылки, баланс блоков кода, пробельные ошибки Git и наличие всех этапов DB1–DB7. Исходные CSV и прототип не изменены.

### Push

`709bca9 Plan database implementation`; отправлен в приватный `origin` вместе с этой записью журнала.

### Решения и ограничения

Трек базы отделён от продуктового трека Аркаши. Условие готовности требует воспроизводимой команды проверки, десяти эталонных SQL-выборок и ручной проверки смысла синтетических данных.

## 02.09.2026 12:26 — Приватная резервная копия проекта

### Цель

Создать приватную резервную копию полного проекта в GitHub.

### План

Проверить состав и размер проекта, отсутствие файлов с ключами, создать приватный репозиторий, зафиксировать все файлы и отправить ветку `main`.

### Выполнено

Инициализирован локальный Git-репозиторий, создан приватный `origin` `<repository>`, добавлены все 74 файла проекта. CLAUDE.md и PLAN.md приведены в соответствие с новым состоянием Git.

### Отклонения

Отклонений нет.

### Проверки

Удалённая ветка `main` перед первым коммитом отсутствовала. Файлов крупнее 90 МБ и файлов с распространёнными сигнатурами приватных ключей и API-токенов не найдено. Размер проекта — 16 МБ.

### Push

`b7c269b Initial private project backup` и `9534639 Document private backup`; отправлены в приватный `origin`.

### Решения и ограничения

Пользователь поручил создать резервную копию. Агент выбрал приватный репозиторий из-за конфиденциальных CSV. Публичная публикация остаётся запрещённой.

## 04.09.2026 22:02 — Backend process fields and assignment-aware OVSYNCH

### Цель

Закрыть backend-пробелы восьми process skills без изменения фронта, публичных contracts, исходных CSV и истории животных.

### Выполнено

Применены `db/migrations/020_process_event_facts.sql`–`024_assignment_aware_ovsynch.sql`. Добавлены event-derived milk windows, two-latest urea/somatic values, process counts, catalog-bound diagnosis/vaccination/procedure dates и assignment-aware current OVSYNCH step context.

`023_batch_event_aggregates.sql` расширяет существующий batching path 019 на поддержанные `EVENT_COUNT` и `EVENT_AGGREGATE`. `024` выбирает active OVSYNCH assignment только для того же животного, исключает effective completion/cancellation и требует exact `assignment_event_id` + effective step event; generic protocol counters не используются как замена.

### Проверки

Live PostgreSQL: 6 ферм; registry вырос до 53 полей на первой ферме. Точечный batch check вернул `INSEMINATION_COUNT_CURRENT_LACTATION=6` и `AVG_DAILY_MILK_7D=32`. После одного refresh projection обработала 51 000 животных за 21,10 секунды; snapshot `stale=false`, lock stampede отсутствует. До 024 было 20,61 секунды.

Boundary SQL для текущих данных: 6 OVSYNCH assignments, все закрыты completion/cancellation; active candidates 0; exact OVSYNCH step links 0; чужие FRESH_COW steps не попали в OVSYNCH context. Filtered fields populated: BASE vaccination — 6 животных, mastitis diagnosis — 1022. LLM e2e не запускался. Синтетические active/other-animal/old-completed/cancelled/superseded cases ещё не добавлены.

### Отклонения и ограничения

Будущая дата расчёта не добавлялась в public tool contract. OVSYNCH fields корректны по связям и effective history, но на текущих данных не имеют положительных active-step fixtures; это не доказательство production coverage. Days-in-milk на дату выбытия, retention denominator, calendar-age months и препаратно-этапные интервалы вакцинаций остаются отдельными пробелами.

### Push

Коммита и push не было.

## 04.09.2026 22:35 — Backend corrective batch and OVSYNCH boundary fixtures

### Выполнено

Добавлены и применены `db/migrations/025_batch_projection_fixes.sql`, `026_ovsynch_start_context.sql`, `027_ovsynch_status_and_formula_version.sql`: исправлены межфермный `EVENT_COUNT`, DESC tie-break, точное семидневное окно `[as_of-7, as_of-1]`, assignment-aware OVSYNCH start/day/step и статусный predicate `status_code != DO_NOT_INSEMINATE`. Frontend и public contracts не менялись.

Добавлен `scripts/assignment-aware-ovsynch-boundary.sql`. Rollback-fixture проверяет positive active assignment, day 10 (`start+9`), `AI` exclusion, чужой animal/assignment с поздним step, completion, cancellation, superseding и voiding; все assertions PASS, транзакция завершена `ROLLBACK`.

### Проверки и ограничения

После `027` refresh обработал 51 000 животных за 19,45 секунды; `animal_rule_projection_snapshot.stale=false`. Независимая raw SQL сверка текущего `INSEMINATION_COUNT_CURRENT_LACTATION` дала 0 расхождений. Локальные 8 representative queries/tool checks PASS; LLM e2e текущего запуска blocked auto-review и не заявляется. Остаются vaccine product/stage intervals, DIM at exit, retention denominator, calendar months и future `asOf` contract.

### Push

Коммита и push не было.

## Entry template

При добавлении записи использовать следующую структуру; текст в блоке — инструкция по заполнению, не запись о выполненной работе.

```text
ДД.ММ.ГГГГ ЧЧ:ММ — Краткое название сессии

Цель
Какой результат требовался пользователю.

План
Что собирались выполнить в этой сессии.

Выполнено
Фактические изменения, ссылки на файлы и пункты PLAN.md.

Отклонения
Что изменилось относительно плана и почему.

Проверки
Какие проверки выполнены, их результаты и что не проверено.

Push
Хеш и название коммита либо указание, что коммита и push не было.

Решения и ограничения
Что делал агент, что решил пользователь, что остаётся нерешённым.
```
